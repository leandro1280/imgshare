<!-- views/image.hbs -->

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h2>{{image.title}}</h2>
    </div>{{image.filename}}
    <div class="card-body">
        <img src="/public/upload/" alt="{{image.title}}" class="img-fluid">
        <p>{{image.description}}</p>
    </div>
</div>
